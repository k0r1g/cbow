__pycache__/
*.py[cod]
*.so
*.pyd

# 📦 Virtual environment / dependencies
.venv/
env/
venv/
ENV/
*.egg-info/
dist/
build/

# 💥 Model checkpoints (Word2Vec + Regressor)
checkpoints/
*.pth
*.npy
*.word2vec
*.bin
*.model

# 📊 Logs
*.log
nohup.out

# 🤖 Token and vocab pickles
*.pkl

# 📁 Hugging Face cache
hf_models/
# Note: Cannot specify home directory paths like ~/.cache/huggingface/ in .gitignore directly.
# Users should configure global gitignore or ensure cache is outside the repo.

# ⚙️ Jupyter checkpoints (optional)
.ipynb_checkpoints/

# 📂 VS Code workspace config (optional but common)
.vscode/

# 🔒 System / OS files
.DS_Store
Thumbs.db

# Existing user rules (retained below)
# hacker_news_titles # Assumed directory names?
# hacker_news_text
# text8
# .env
# *$py.class # Already covered by *.py[cod]? Retained for safety.

# wandb specific logs/cache
wandb/

# Note: Ignoring ~/.huggingface/ is not typically done in a project .gitignore,
# it should be handled globally by the user if desired.